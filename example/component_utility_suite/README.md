# Arcane utility component suite

This synthetic page demonstrates the shared calculator, weather widget, screen-capture surface, and event-driven `ApiModelDatabase`. It uses an injected in-memory API response and synthetic weather records. It never requests display permission until the user presses a capture button.

Serve the repository root and open `/example/component_utility_suite/index.html`.
