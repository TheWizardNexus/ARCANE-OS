# Communications components

This example demonstrates the shared unified inbox and conversation view with two in-memory provider adapters. Parent applications select providers, persist connection settings, and route authentication. Shared components render normalized records and emit `thread-select`, `inbox-refresh`, and `communication-send` events.

Serve the repository root and open `/example/component_communications/index.html`.
