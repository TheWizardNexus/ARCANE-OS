const tool=document.querySelector('#captureTool'),save=document.querySelector('#saveCapture');let last=null;tool.addEventListener('screen-capture-result',event=>{last=event.detail.result;save.disabled=false});save.addEventListener('click',()=>{if(!last)return;const url=URL.createObjectURL(last.blob),link=document.createElement('a'),stamp=new Date().toISOString().replace(/[:.]/g,'-');link.href=url;link.download=`arcane-capture-${stamp}.${last.extension}`;link.click();setTimeout(()=>URL.revokeObjectURL(url),0)});
