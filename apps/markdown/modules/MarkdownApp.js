import DBLS from '../../../arcane/modules/DBLS.js';

const storage=new DBLS(),editor=document.querySelector('#editor'),key='draft';function saved(){const value=storage.get(key);return value&&typeof value==='object'?value:{}}function configure(){const draft=saved();editor.configure({clearOnSave:false,initialTitle:draft.title||'',initialValue:draft.markdown||'',saveLabel:'Save draft'});editor.save=async value=>{storage.set(key,value);return {saved:true}}}editor.addEventListener('markdown-editor-ready',configure,{once:true});document.querySelector('#download').addEventListener('click',()=>{const blob=new Blob([editor.value||''],{type:'text/markdown;charset=utf-8'}),url=URL.createObjectURL(blob),link=document.createElement('a');link.href=url;link.download=`${(editor.entryTitle||'arcane-note').replace(/[^a-z0-9_-]+/gi,'-').replace(/^-|-$/g,'')||'arcane-note'}.md`;link.click();setTimeout(()=>URL.revokeObjectURL(url),0)});if(editor.ready)configure();
