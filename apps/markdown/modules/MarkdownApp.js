const editor=document.querySelector('#editor'),key='arcane.markdown.draft';function saved(){try{return JSON.parse(localStorage.getItem(key)||'{}')}catch{return {}}}function configure(){const draft=saved();editor.configure({clearOnSave:false,initialTitle:draft.title||'',initialValue:draft.markdown||'',saveLabel:'Save draft'});editor.save=async value=>{localStorage.setItem(key,JSON.stringify(value));return {saved:true}}}editor.addEventListener('markdown-editor-ready',configure,{once:true});document.querySelector('#download').addEventListener('click',()=>{const blob=new Blob([editor.value||''],{type:'text/markdown;charset=utf-8'}),url=URL.createObjectURL(blob),link=document.createElement('a');link.href=url;link.download=`${(editor.entryTitle||'arcane-note').replace(/[^a-z0-9_-]+/gi,'-').replace(/^-|-$/g,'')||'arcane-note'}.md`;link.click();setTimeout(()=>URL.revokeObjectURL(url),0)});if(editor.ready)configure();
